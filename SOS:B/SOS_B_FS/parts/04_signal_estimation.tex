% ============================================================
%  04 · Signalschätzung / Estimation
%      Quelle: 04_signal_estimation.pdf + 01_Grundlagen.pdf
% ============================================================

\bereich[cSchaetz]{Schätztheorie — Grundbegriffe}
\begin{formelreihe}
  \infoblock{Schätzer: Algorithmus, der aus Beobachtungsvektor $\mathbf{X}$
    (Zufallsvariable) einen Schätzwert $\hat{\theta}$ für den
    deterministischen Parametervektor $\boldsymbol{\theta}$ liefert.} &
  \formelblock{Schätzfehler}{e=\theta-\hat{\theta}} &
  \formelblock{MSE (Skalar)}{\mathrm{MSE}=E\{e^2\}=E\{(\theta-\hat{\theta})^2\}} \\[0.3em]
  \formelblock{MSE (Vektor)}{\mathrm{MSE}=E\{\mathbf{e}\cdot\mathbf{e}^T\}
    =E\{(\boldsymbol{\theta}-\hat{\boldsymbol{\theta}})
    (\boldsymbol{\theta}-\hat{\boldsymbol{\theta}})^T\}} &
  \formelblock{Entwurfsziel}{\mathrm{MSE}\to\min\quad\text{(MMSE)}} &
  \formelblock{MSE-Zerlegung}{\mathrm{MSE}=\mathrm{Bias}^2+\mathrm{Var}(\hat{\theta})} \\
\end{formelreihe}
\miniexample{MMSE — redundante Messung: $y_i=x+n_i$ ($n_i$ unkorr.), $\hat x=k_1y_1+k_2y_2$}{%
  Erwartungstreu $\Rightarrow k_1+k_2=1$; Varianz minimieren $\Rightarrow$ Gewicht $\propto$ inverse
  Rauschvarianz:\;
  $k_1=\dfrac{\sigma_{n_2}^2}{\sigma_{n_1}^2+\sigma_{n_2}^2}$,\quad
  $k_2=\dfrac{\sigma_{n_1}^2}{\sigma_{n_1}^2+\sigma_{n_2}^2}$.\;
  Z.\,B.\ $\sigma_{n_1}^2{=}1,\,\sigma_{n_2}^2{=}3\Rightarrow k_1{=}\tfrac34,\,k_2{=}\tfrac14$
  (kleinere Rauschvarianz $\to$ größeres Gewicht — Grundidee des Kalman-Gains).}
\bereichende

\bereich[cSchaetz]{Bewertungskriterien \& Eigenschaften}
\begin{formelreihe}
  \formelblock{Bias (syst.\ Fehler)}{b=E\{\hat{\theta}\}-\theta} &
  \formelblock{Kovarianz der Schätzung}{C_{\hat{\theta}}
    =E\!\left\{(\hat{\theta}{-}E\{\hat{\theta}\})
    (\hat{\theta}{-}E\{\hat{\theta}\})^T\right\}} &
  \formelblock{Erwartungstreu (unbiased)}{E\{\hat{\theta}\}=\theta
    \;\Leftrightarrow\; b=0} \\[0.3em]
  \formelblock{Asympt.\ erwartungstreu}{\lim_{N\to\infty}E\{\hat{\theta}\}=\theta} &
  \formelblock{Konsistent}{\lim_{N\to\infty}P(|\hat{\theta}-\theta|>\epsilon)=0} &
  \formelblock{Effizient}{C_{\hat{\theta}^*}\ge C_{\hat{\theta}_{\min}}\;\forall\,\hat{\theta}^*} \\
\end{formelreihe}
\bereichende

\bereich[cSchaetz]{Mittelwert- \& Varianzschätzer}
\begin{formelreihe}
  \formelblock{Stichprobenmittel (erw.-treu)}{\hat{\mu}_X
    =\dfrac{1}{N}\sum_{i=1}^{N}x_i,\quad E\{\hat{\mu}_X\}=\mu_X} &
  \formelblock{Varianz — biased}{\hat{\sigma}_{X,*}^2
    =\dfrac{1}{N}\sum_{i=1}^{N}(x_i-\hat{\mu}_X)^2} &
  \beispielblock{Bias der biased Varianz}{E\{\hat{\sigma}_{X,*}^2\}
    =\sigma_X^2\cdot\dfrac{N-1}{N}\;\neq\sigma_X^2} \\[0.3em]
  \formelblock{Varianz — unbiased}{\hat{\sigma}_X^2
    =\dfrac{1}{N-1}\sum_{i=1}^{N}(x_i-\hat{\mu}_X)^2} &
  \formelblock{Korrektur}{\hat{\sigma}_X^2
    =\dfrac{N}{N-1}\,\hat{\sigma}_{X,*}^2,\quad
    E\{\hat{\sigma}_X^2\}=\sigma_X^2} &
  \infoblock{Biased: asympt.\ erw.-treu\\
    ($\lim_{N\to\infty}\tfrac{N-1}{N}=1$).\\
    $\mu_X$ bekannt $\Rightarrow$ Nenner $N$ korrekt.} \\
\end{formelreihe}
\bereichende

\bereich[cSchaetz]{Korrelationsschätzer}
\begin{formelreihe}
  \formelblock{Kreuzkorrelation (def.)}{\varphi_{xy}[\kappa]
    =E\{X[k{+}\kappa]\cdot Y[k]\}} &
  \formelblock{Unnormierte Summe}{\hat{c}_{xy}[\kappa]
    =\sum_{k=0}^{N-\kappa-1}X[k{+}\kappa]\cdot Y[k]\quad(\kappa\ge0)} &
  \formelblock{Biased-Schätzer}{\hat{\varphi}_{xy}^*[\kappa]
    =\dfrac{1}{N}\,\hat{c}_{xy}[\kappa]} \\[0.3em]
  \formelblock{Unbiased-Schätzer}{\hat{\varphi}_{xy}[\kappa]
    =\dfrac{1}{N-|\kappa|}\,\hat{c}_{xy}[\kappa]} &
  \infoblock{Bevorzugt für kleine $|\kappa|$.\\
    Schätzfehler steigt für $|\kappa|\to N$.} &
  \\
\end{formelreihe}
\bereichende

% ==========================================================
%  WIENER-FILTER  (Theorie + zugehöriges Rezept)
% ==========================================================

\bereich[cSchaetz]{Wiener-Filter — Grundprinzip}
\begin{formelreihe}
  \infoblock{$x(t)$: $s(t)$ verzerrt (LTI) $+$ Rauschen.\\
    $H$: min.\ Fehlerleistung $E\{|e|^2\}$.\\
    $e(t)=y(t)-s(t)$} &
  \formelblock{LDS des Fehlers}{\Phi_{ee}
    =\Phi_{xx}|H|^2-\Phi_{xs}H-\Phi_{sx}H^*+\Phi_{ss}} &
  \formelblock{Wiener-Filter (kontinuierlich)}{H(j\omega)
    =\dfrac{\Phi_{sx}(\omega)}{\Phi_{xx}(\omega)}
    =\dfrac{\Phi_{xs}^*(\omega)}{\Phi_{xx}(\omega)}} \\[0.3em]
  \formelblock{Wiener-Filter (zeitdiskret)}{H(e^{j\Omega})
    =\dfrac{\Phi_{sx}(\Omega)}{\Phi_{xx}(\Omega)}
    =\dfrac{\Phi_{xs}^*(\Omega)}{\Phi_{xx}(\Omega)}} &
  \beispielblock{Gestörter Kanal: $x=G*s+n$, $n\perp s$}{%
    \Phi_{xx}=\Phi_{ss}|G|^2+\Phi_{nn}\\[0.15em]
    \Phi_{sx}=\Phi_{ss}\cdot G^*(j\omega)\\[0.15em]
    H=\dfrac{\Phi_{ss}\cdot G^*}{\Phi_{ss}|G|^2+\Phi_{nn}}} &
  \infoblock{Einschränkungen:\\
    -- unendl.\ Beobachtungsdauer nötig\\
    -- Prozesse müssen stationär sein\\
    -- Einschwingvorgang\\[0.1em]
    $\to$ Kalman-Filter für endliche\\
    Beobachtung, zeitvariante Systeme} \\
\end{formelreihe}
\par\vspace{0.3em}\centering
{\scriptsize\scshape\color{gray!65!black} Strukturbild · Wiener-Filter (MSE-optimal, stationär)}\par\vspace{0.12em}
\begin{tikzpicture}[font=\scriptsize,color=teal!72!black,line width=0.6pt,
    blk/.style={draw,fill=white,inner sep=2pt,minimum height=4.6mm,minimum width=5.2mm},
    sj/.style={draw,circle,inner sep=0pt,minimum size=3.8mm}]
  % --- Strecke (Verzerrung G + Rauschen n)  |  Rekonstruktion H  |  Fehlerbildung
  \node      (s)  at (0,1.3)    {$s(t)$};
  \node[blk] (G)  at (1.35,1.3) {$G$};
  \node[sj]  (sn) at (2.65,1.3) {$+$};
  \node[blk] (H)  at (4.55,1.3) {$H$};
  \node[sj]  (se) at (6.6,1.3)  {};
  \node      (e)  at (7.55,1.3) {$e(t)$};
  \node      (n)  at (2.65,2.25){$n(t)$};
  % --- Labels auf Leitungen
  \node[above=0pt,font=\tiny] at (3.55,1.28){$x(t)$};
  \node[above=0pt,font=\tiny] at (5.7,1.28) {$\hat s(t)$};
  \node[font=\tiny] at (6.27,1.52){$+$};
  \node[font=\tiny] at (6.62,0.96){$-$};
  % --- Verbindungen Hauptpfad
  \draw[->] (s)--(G);
  \draw[->] (G)--(sn);
  \draw[->] (n)--(sn);
  \draw[->] (sn)-- node[pos=0.45,coordinate](xx){} (H);
  \draw[->] (H)--(se);
  \draw[->] (se)--(e);
  % --- Nutzsignal s(t) als Soll in se (−)
  \coordinate (sbr) at (0.55,1.3);
  \fill (sbr) circle (0.7pt);
  \draw (sbr)--(0.55,-0.45);
  \draw (0.55,-0.45)--(6.6,-0.45);
  \draw[->] (6.6,-0.45)-- node[right,font=\tiny,pos=0.12]{$s(t)$ (Soll)} (se);
\end{tikzpicture}\par
\vspace{0.12em}
\parbox{0.97\linewidth}{\footnotesize $x=G*s+n\to$ Rekonstruktionsfilter $H\to$ Schätzung $\hat s(t)$.\;
Fehler $e{=}\hat s-s$ ggü.\ (unbekannt) Nutzsignal $s$.\;
$\min E\{|e|^2\}\Rightarrow$ \emph{Orthogonalität} $e\perp x\Rightarrow H{=}\Phi_{sx}/\Phi_{xx}$.\;
\itshape vs.\ Kalman: festes (zeitinvar.)\ $H$, kein Zustand/Rekursion.}\par\raggedright
\bereichende

\bereich[cSchaetz]{REZEPT · Wiener-Filter als MSE-Minimierung im Zeitbereich}
\noindent\footnotesize
Filter $=$ \emph{Konstante} $h_0$ (kein LDS gefordert).\; Ziel: $E\{e^2\}\to\min$.
\begin{rezept}
  \rs{Schätzwert hinschreiben: $\hat s(t)=h_0\cdot x(t)$, dabei $x(t)$ durch
      $s$, verzögerte $s$ und Rauschen ausdrücken.}
  \rs{Fehler $e(t)=s(t)-\hat s(t)$ einsetzen und nach Termen ordnen.}
  \rs{$E\{e^2\}$ ausmultiplizieren; nutze $E\{s(t)s(t{-}\tau)\}=\varphi_{ss}(\tau)$,
      $E\{s\cdot n\}=0$, $E\{n^2\}=\varphi_{nn}(0)$.}
  \rs{Ergebnis ist Parabel $E\{e^2\}=\alpha\,h_0^2+\beta\,h_0+\gamma$.}
  \rs{$\dfrac{\mathrm d}{\mathrm d h_0}E\{e^2\}=0 \Rightarrow h_0=-\dfrac{\beta}{2\alpha}$.}
\end{rezept}
\miniexample{$\hat s=h_0\,(s(t)+s(t{-}1)+n)$, $\varphi_{ss}(0){=}2$, $\varphi_{ss}(1){=}1$, $\varphi_{nn}(0){=}3$, $s\perp n$}{%
  $e=s(1{-}h_0)-h_0 s(t{-}1)-h_0 n$\;
  $\Rightarrow E\{e^2\}=9\,h_0^2-6\,h_0+2$\;
  $\Rightarrow h_0=\dfrac{6}{2\cdot9}=\dfrac13$.}
\bereichende

% ==========================================================
%  KALMAN-FILTER  (Systemmodell + Algorithmus + Endwert, je mit Rezept)
% ==========================================================

\bereich[cSchaetz]{Kalman-Filter — Systemmodell}
\begin{formelreihe}
  \formelblock{Zustandsdifferenzengleichung}{x[k{+}1]=A[k]x[k]+B[k]u[k]+G[k]z[k]\\[0.1em]
    y[k]=C[k]x[k]+n[k]} &
  \infoblock{$x[k]$: Zustandsvektor (unbekannt)\\
    $u[k]$: bekannter Eingang\\
    $z[k]$: Systemrauschen (weiß, mw-frei)\\
    $n[k]$: Messrauschen (weiß, mw-frei)\\[0.1em]
    $\varphi_{zz}[\kappa,k]=\delta[\kappa]\cdot Z_0[k]$\\
    $\varphi_{nn}[\kappa,k]=\delta[\kappa]\cdot N_0[k]$} &
  \formelblock{Schätzfehler \& Ziel}{e[k]=\hat{x}[k]-x[k]\\[0.1em]
    F[k]=E\{e[k]\cdot e^T[k]\}\quad\text{(Fehlerkorr.-Matrix)}\\[0.1em]
    \mathrm{trace}\{F[k]\}\to\min} \\
\end{formelreihe}
\bereichende

\bereich[cSchaetz]{REZEPT · Zustandsraum aus Blockschaltbild ablesen}
\noindent\footnotesize
Jeder $z^{-1}$ $=$ Zustand $x_i$: Ausgang $x_i[k]$, Eingang $x_i[k{+}1]$.\;
Pro Verzögerer-Eingang: Summe aller einlaufenden Pfade.
\begin{rezept}
  \rs{Verzögerer durchnummerieren $x_1\dots x_n$ (Ausgang $=x_i[k]$).}
  \rs{Für jeden Eingang $x_i[k{+}1]$: alle einlaufenden Zweige addieren
      (Rückkopplungsfaktor $\to$ Eintrag in $\mathbf A$, Pfad von $u[k]\to\mathbf B$,
      Pfad von $z[k]\to\mathbf G$).}
  \rs{Ausgangsgleichung $y[k]=\mathbf C\,\mathbf x[k]+n[k]$: welcher Zustand (mit
      welchem Faktor) liegt am Messpunkt? $\to\mathbf C$. Additives Rauschen $n[k]$
      am Ausgang.}
  \rs{Reihenfolge im Vektor frei, aber konsistent halten (Eingang von $x_i$ links,
      Ausgang von $x_i$ rechts vom $z^{-1}$).}
\end{rezept}
\fallstrick{Skalierungsblock \emph{nach} dem Verzögerer (z.\,B.\ $\cdot 2$ am
  Messabgriff) gehört in $\mathbf C$, nicht in $\mathbf A$ (Fall $C{\neq}1$).\;
  Rauschpfad $z[k]$ läuft i.\,d.\,R.\ über einen eigenen Faktor in $\mathbf G$ —
  nicht mit $\mathbf B$ verwechseln.}
\par\vspace{0.3em}
\centering
\begin{tikzpicture}[font=\scriptsize,color=teal!72!black,line width=0.6pt,
    blk/.style={draw,fill=white,inner sep=2pt,minimum height=4.6mm,minimum width=5mm},
    sj/.style={draw,circle,inner sep=0pt,minimum size=3.7mm}]
  % --- Hauptpfad:  (+) -> z^{-1} -> x[k] -> C -> (+) -> y
  \node[sj]  (sig)  at (2.5,1.2) {$+$};
  \node[blk] (vz)   at (3.8,1.2) {$z^{-1}$};
  \coordinate        (tap)  at (5.1,1.2);
  \node[blk] (C)    at (6.1,1.2) {$\mathbf C$};
  \node[sj]  (sig2) at (7.3,1.2) {$+$};
  \node      (y)    at (8.1,1.2) {$y[k]$};
  % --- Eingänge u (über B) und z (über G)
  \node      (u)    at (0,2.05)  {$u[k]$};
  \node[blk] (B)    at (1.25,2.05){$\mathbf B$};
  \node      (z)    at (0,1.2)   {$z[k]$};
  \node[blk] (G)    at (1.25,1.2){$\mathbf G$};
  % --- Messrauschen n
  \node      (n)    at (7.3,2.05){$n[k]$};
  % --- Rückführung A
  \node[blk] (A)    at (3.8,0.35){$\mathbf A$};
  % --- Verbindungen
  \draw[->] (u)--(B);
  \draw (B)--(2.5,2.05); \draw[->] (2.5,2.05)--(sig);
  \draw[->] (z)--(G);
  \draw[->] (G)--(sig);
  \draw[->] (sig)-- node[above,font=\tiny,pos=0.5]{$x[k{+}1]$} (vz);
  \draw (vz)--(tap); \fill (tap) circle (0.7pt);
  \node[above=0pt,font=\tiny] at (4.55,1.18){$x[k]$};
  \draw[->] (tap)--(C);
  \draw[->] (C)--(sig2);
  \draw[->] (sig2)--(y);
  \draw[->] (n)--(sig2);
  \draw (tap)--(5.1,0.35);
  \draw[->] (5.1,0.35)--(A);
  \draw (A)--(2.5,0.35);
  \draw[->] (2.5,0.35)--(sig);
\end{tikzpicture}\par
\vspace{0.15em}
{\footnotesize Block $=$ Matrix: $\mathbf A$ Rückführung, $\mathbf B$ Eingang $u$,
$\mathbf G$ Rauschpfad $z$, $\mathbf C$ Messabgriff.\; $z^{-1}$ hält Zustand
($x[k{+}1]\!\to\!x[k]$); $n[k]$ additiv am Ausgang.}\par\vspace{0.2em}
\miniexample{System 1.\ Ordnung mit Messverstärkung ($C{\neq}1$)}{%
  $x[k{+}1]=0{,}9\,x[k]+0{,}5\,u[k]+0{,}3\,z[k]$,\quad $y[k]=2\,x[k]+n[k]$\\
  $\Rightarrow A{=}0{,}9,\;B{=}0{,}5,\;G{=}0{,}3,\;C{=}2$.}
\bereichende

\bereich[cSchaetz]{Kalman-Filter — Algorithmus (beide Varianten)}
\noindent\footnotesize
Start: $\mathbf F[0]{=}\mathbf 0$, $\hat{\mathbf x}[0]{=}\mathbf 0$ (sofern nicht anders).\;
Reihenfolge je Schritt $k$: $R\to K/M\to\hat x\to F$.\;
zeitvariant nur $K/M,F$; $A,B,C,G$ konst.\\[0.15em]
{\scshape\color{gray!65!black} $Z_0,N_0$?} $=$ Leistung der weißen Prozesse $z$ (System) / $n$ (Messung), ACF-Wert bei $\kappa{=}0$:\;
$\varphi_{zz}{=}Z_0\delta[\kappa]$, $\varphi_{nn}{=}N_0\delta[\kappa]$ — direkt ablesen
(z.\,B.\ $\varphi_{zz}{=}3\delta\Rightarrow Z_0{=}3$, $\varphi_{nn}{=}2\delta\Rightarrow N_0{=}2$).
\par\vspace{0.35em}
{\setlength{\tabcolsep}{0pt}\renewcommand{\arraystretch}{0.9}%
\noindent
\begin{tabular}{@{}p{0.485\linewidth}@{\hspace{0.03\linewidth}}p{0.485\linewidth}@{}}
  \parbox[t]{\linewidth}{\scriptsize
    {\scshape\color{gray!65!black} Welche Variante?}\\
    \itshape kein extra Korrekturpfad $\to$ \emph{ohne};\;
    Innovationspfad $e{=}y{-}\hat y$ direkt auf $\hat x^{+}$ $\to$ \emph{mit}.} &
  \parbox[t]{\linewidth}{\scriptsize
    {\scshape\color{gray!65!black} Alt.\ Nomenklatur}\\
    \itshape $\Sigma\,{\hat=}\,M$, $\Lambda\,{\hat=}\,$Rückführ., $\Phi,\Psi\,{\hat=}\,A$/Verzög.\ — Struktur identisch.} \\
\end{tabular}\par
\vspace{0.5em}\noindent
\begin{tabular}{@{}p{0.485\linewidth}@{\hspace{0.03\linewidth}}p{0.485\linewidth}@{}}
  \parbox[t]{\linewidth}{%
    {\scriptsize\scshape\color{gray!65!black} Variante A — OHNE Innovationsterm (a-priori $\hat x[k]$)}\par\vspace{0.18em}
    \begin{rezept}
      \rs{$R[k]=N_0+C\,F[k]\,C^{T}$}
      \rs{$K[k]=A\,F[k]\,C^{T}\,R^{-1}[k]$}
      \rs{$\hat x[k{+}1]=A\,\hat x[k]+B\,u[k]+K[k]\,(y[k]-C\,\hat x[k])$}
      \rs{$F[k{+}1]=A\,F[k]\,A^{T}-K[k]\,R[k]\,K^{T}[k]+G\,Z_0\,G^{T}$}
    \end{rezept}} &
  \parbox[t]{\linewidth}{%
    {\scriptsize\scshape\color{gray!65!black} Variante B — MIT Innovationsterm (a-posteriori $\hat x^{+}[k]$)}\par\vspace{0.18em}
    \begin{rezept}
      \rs{$R[k]=N_0+C\,F[k]\,C^{T}$}
      \rs{$M[k]=F[k]\,C^{T}\,R^{-1}[k]$ (Gewichtungsm.),\; $K[k]=A\,M[k]$}
      \rs{$\hat x^{+}[k]=\hat x[k]+M[k]\,(y[k]-C\,\hat x[k])$}
      \rs{$\hat x[k{+}1]=A\,\hat x^{+}[k]+B\,u[k]$}
      \rs{$F[k{+}1]=A\,F[k]\,A^{T}-K[k]\,R[k]\,K^{T}[k]+G\,Z_0\,G^{T}$}
    \end{rezept}} \\
\end{tabular}\par}
\par\vspace{0.3em}\noindent
{\scriptsize\scshape\color{gray!65!black} Skalarfall ($n{=}1$, $C$ skalar) — geschlossene Form}\par\nopagebreak\vspace{0.12em}
\noindent\footnotesize
$R[k]=N_0+C^2 f[k]$,\qquad
$\displaystyle K[k]=\frac{A\,C\,f[k]}{N_0+C^2 f[k]}\;\;(=A\,M[k],\;\; M[k]=\tfrac{C f[k]}{N_0+C^2 f[k]})$,\qquad
$\displaystyle f[k{+}1]=A^2 f[k]-\frac{A^2 C^2 f^2[k]}{N_0+C^2 f[k]}+G^2 Z_0$.\\[0.2em]
\emph{Für $C{=}1$:} $\;K[k]=\dfrac{A\,f[k]}{N_0+f[k]}$,\quad
$f[k{+}1]=A^2 f[k]-\dfrac{A^2 f^2[k]}{N_0+f[k]}+G^2 Z_0$.\;
\itshape (nur skalar; Matrixfall $\to$ Einzelschritte oben).\upshape\par\vspace{0.15em}
\fallstrick{Erst $R$, dann $K/M$, dann $\hat x$, zuletzt $F$ — Reihenfolge nicht
  vertauschen.\; $F[0]{=}0\Rightarrow K[0]{=}0\Rightarrow F[1]=G Z_0 G^{T}$ (immer!).\;
  Bei $C{\neq}1$: $C F C^{T}$ und $C\hat x$ nicht vergessen.\;
  $u[k]=\cos(k\pi)=(-1)^k$ erzeugt die Folge $1,-1,1,\dots$}

\bereichende

\bereich[cSchaetz]{Kalman-Filter — Strukturbilder (Variante A \& B)}
% ---- Strukturbilder der Filterstruktur (Klausur: zu vorgeg. System zeichnen) ----
\par\vspace{0.2em}\centering
{\scriptsize\scshape\color{gray!65!black} Strukturbild · Variante A — OHNE Innovationsterm (a-priori $\hat x[k]$)}\par\vspace{0.12em}
\begin{tikzpicture}[font=\scriptsize,color=teal!72!black,line width=0.6pt,
    blk/.style={draw,fill=white,inner sep=2pt,minimum height=4.6mm,minimum width=5.2mm},
    sj/.style={draw,circle,inner sep=0pt,minimum size=3.8mm}]
  % --- Hauptpfad
  \node      (y)   at (0,1.4)    {$y[k]$};
  \node[sj]  (s1)  at (1.25,1.4) {};
  \node[blk] (K)   at (2.6,1.4)  {$\mathbf K[k]$};
  \node[sj]  (s2)  at (4.25,1.4) {$+$};
  \node[blk] (vz)  at (5.55,1.4) {$z^{-1}$};
  \coordinate      (tap) at (6.75,1.4);
  \node      (xh)  at (7.65,1.4) {$\hat{\mathbf x}[k]$};
  % --- Eingang u über B (von oben in s2)
  \node      (u)   at (3.1,2.35) {$u[k]$};
  \node[blk] (B)   at (4.25,2.35){$\mathbf B$};
  % --- Rückführung A (in s2)
  \node[blk] (A)   at (5.1,0.45) {$\mathbf A$};
  % --- Messabgriff C (Rückführung in s1)
  \node[blk] (C)   at (3.5,-0.55){$\mathbf C$};
  % --- Vorzeichen an s1
  \node[font=\tiny] at (0.92,1.62){$+$};
  \node[font=\tiny] at (1.18,1.08){$-$};
  % --- Verbindungen
  \draw[->] (y)--(s1);
  \draw[->] (s1)-- node[above,font=\tiny,pos=0.5]{$e[k]$} (K);
  \draw[->] (K)--(s2);
  \draw[->] (s2)-- node[above,font=\tiny,pos=0.46]{$\hat{\mathbf x}[k{+}1]$} (vz);
  \draw (vz)--(tap); \fill (tap) circle (0.7pt);
  \draw[->] (tap)--(xh);
  \draw[->] (u)--(B);
  \draw[->] (B)--(s2);
  % A-Rückführung
  \draw (tap)--(6.75,0.45); \fill (6.75,0.45) circle (0.7pt);
  \draw[->] (6.75,0.45)--(A);
  \draw (A)--(4.25,0.45);
  \draw[->] (4.25,0.45)--(s2);
  % C-Rückführung
  \draw (6.75,0.45)--(6.75,-0.55);
  \draw[->] (6.75,-0.55)--(C);
  \draw (C)--(1.25,-0.55);
  \draw[->] (1.25,-0.55)-- node[right,font=\tiny,pos=0.22]{$\mathbf C\hat{\mathbf x}[k]$} (s1);
\end{tikzpicture}\par
\vspace{0.12em}
\parbox{0.97\linewidth}{\footnotesize Eine Summierstelle:
$\hat{\mathbf x}[k{+}1]=\mathbf A\hat{\mathbf x}[k]+\mathbf B u[k]
+\mathbf K[k]\,\underbrace{(y[k]-\mathbf C\hat{\mathbf x}[k])}_{e[k]\,=\,\text{Fehler/Innov.}}$.\;
$\mathbf A$ im \emph{Rückführzweig}; $\mathbf K e$ in dieselbe Summe wie $\mathbf A\hat{\mathbf x},\mathbf B u$.\; kein a-posteriori-Knoten.}\par
\vspace{0.55em}
{\scriptsize\scshape\color{gray!65!black} Strukturbild · Variante B — MIT Innovationsterm (a-posteriori $\hat x^{+}[k]$)}\par\vspace{0.12em}
\begin{tikzpicture}[font=\scriptsize,color=teal!72!black,line width=0.6pt,
    blk/.style={draw,fill=white,inner sep=2pt,minimum height=4.6mm,minimum width=5.2mm},
    sj/.style={draw,circle,inner sep=0pt,minimum size=3.8mm}]
  % --- Hauptpfad: s1(Innov.) -> M -> sc(Korrektur, x^+) -> A -> s2(Prädiktion) -> z^{-1}
  \node      (y)   at (0,1.4)    {$y[k]$};
  \node[sj]  (s1)  at (1.25,1.4) {};
  \node[blk] (M)   at (2.55,1.4) {$\mathbf M[k]$};
  \node[sj]  (sc)  at (3.95,1.4) {$+$};
  \node[blk] (A)   at (5.25,1.4) {$\mathbf A$};
  \node[sj]  (s2)  at (6.55,1.4) {$+$};
  \node[blk] (vz)  at (7.85,1.4) {$z^{-1}$};
  \coordinate      (tap) at (9.0,1.4);
  \node      (xh)  at (9.9,1.4)  {$\hat{\mathbf x}[k]$};
  \node[above=1pt,font=\tiny] at (4.6,1.42){$\hat{\mathbf x}^{+}[k]$};
  % --- Eingang u über B (in s2)
  \node      (u)   at (5.4,2.35) {$u[k]$};
  \node[blk] (B)   at (6.55,2.35){$\mathbf B$};
  % --- Messabgriff C (Rückführung in s1)
  \node[blk] (C)   at (4.0,-0.55){$\mathbf C$};
  % --- Vorzeichen
  \node[font=\tiny] at (0.92,1.62){$+$};
  \node[font=\tiny] at (1.18,1.08){$-$};
  \node[font=\tiny] at (3.7,1.1)  {$+$};
  % --- Verbindungen forward
  \draw[->] (y)--(s1);
  \draw[->] (s1)-- node[above,font=\tiny,pos=0.5]{$e[k]$} (M);
  \draw[->] (M)--(sc);
  \draw[->] (sc)--(A);
  \draw[->] (A)--(s2);
  \draw[->] (s2)-- node[above,font=\tiny,pos=0.46]{$\hat{\mathbf x}[k{+}1]$} (vz);
  \draw (vz)--(tap); \fill (tap) circle (0.7pt);
  \draw[->] (tap)--(xh);
  \draw[->] (u)--(B);
  \draw[->] (B)--(s2);
  % --- direkte x̂[k]-Rückführung in sc (+1)
  \draw (tap)--(9.0,0.5); \fill (9.0,0.5) circle (0.7pt);
  \draw (9.0,0.5)--(3.95,0.5);
  \draw[->] (3.95,0.5)--(sc);
  % --- C-Rückführung in s1
  \draw (9.0,0.5)--(9.0,-0.55);
  \draw[->] (9.0,-0.55)--(C);
  \draw (C)--(1.25,-0.55);
  \draw[->] (1.25,-0.55)-- node[right,font=\tiny,pos=0.22]{$\mathbf C\hat{\mathbf x}[k]$} (s1);
\end{tikzpicture}\par
\vspace{0.12em}
\parbox{0.97\linewidth}{\footnotesize Zwei Stufen, \emph{expliziter} a-posteriori-Knoten:\;
\textbf{Korrektur} $\hat{\mathbf x}^{+}[k]=\hat{\mathbf x}[k]+\mathbf M[k]\,e[k]$ ($e{=}y-\mathbf C\hat{\mathbf x}$),\;
\textbf{Prädiktion} $\hat{\mathbf x}[k{+}1]=\mathbf A\hat{\mathbf x}^{+}[k]+\mathbf B u[k]$.\;
$\mathbf A$ jetzt im \emph{Vorwärtszweig} (hinter $\hat{\mathbf x}^{+}$), nicht Rückführung;\; $\mathbf K=\mathbf A\mathbf M$.}\par\raggedright
\bereichende

\bereich[cSchaetz]{Kalman-Filter — Stationärer Endwert \& Dimensionen}
\begin{formelreihe}
  \formelblock{Stationärer Endwert $f_\infty$ (Skalar)}{%
    f_\infty=\dfrac{A^2 N_0\,f_\infty}{N_0+C^2 f_\infty}+G Z_0 G^{T}\\[0.15em]
    \text{quadr.\ Gl.\ lösen, }R_\infty{=}N_0{+}C^2 f_\infty\\[0.1em]
    \Rightarrow K_\infty=\dfrac{A\,C\,f_\infty}{R_\infty}} &
  \parbox[t]{\linewidth}{%
    {\scriptsize\scshape\color{gray!65!black} Endwert bestimmen}\par\vspace{0.05em}
    \begin{rezept}
      \rs{$F[k{+}1]{=}F[k]{=}f_\infty$ in die $F$-Rekursion einsetzen.}
      \rs{$K/M$ durch $f_\infty$ ausdrücken und einsetzen.}
      \rs{Auf $f_\infty^2+p\,f_\infty+q=0$ bringen, quadr.\ Gl.\ lösen.}
      \rs{\emph{Nur positive} Lösung behalten $\to K_\infty$ bzw.\ $M_\infty$.}
    \end{rezept}} &
  \infoblock{\textbf{Endwert existiert?} Ja, falls System LTI \emph{und}
    Rauschen (schwach) stationär (so begründen).\\[0.3em]
    Mit Endwerten init.: kein Einschwingen, Filter zeitinvar.\ $\Rightarrow$ Kalman $=$ Wiener.} \\[0.3em]
  \parbox[t]{\linewidth}{%
    {\scriptsize\scshape\color{gray!65!black} Dimensionen ($n$ Zustände, $1$ Ausgang)}\\[0.1em]
    {\footnotesize
    $\mathbf A:n{\times}n$,\;\; $\mathbf C:1{\times}n$,\;\;
    $\mathbf G:n{\times}q$,\;\; $\mathbf F:n{\times}n$,\;\;
    $R:1{\times}1$,\;\; $\mathbf M[k],\mathbf K[k]:n{\times}1$.\;
    \itshape ($q$ = Anzahl Systemrausch-Quellen $z$.)}} & & \\
\end{formelreihe}
\miniexample{Skalar, ohne Innovation ($A{=}0{,}6$, $C{=}1$, $G{=}0{,}5$, $Z_0{=}2$, $N_0{=}1$)}{%
  $R[k]{=}1{+}f[k]$,\quad $K[k]{=}\dfrac{0{,}6\,f[k]}{1{+}f[k]}$,\quad
  $f[k{+}1]{=}0{,}36\,f[k]-\dfrac{0{,}36\,f^{2}[k]}{1{+}f[k]}+0{,}5$\\[0.25em]
  \begin{tabular}{@{}l|cccc@{}}
    $k$ & 0 & 1 & 2 & 3\\\hline
    $f[k]$ & 0 & 0{,}5 & 0{,}62 & 0{,}6378\\
    $K[k]$ & 0 & 0{,}2 & 0{,}2296 & 0{,}2336
  \end{tabular}\quad
  $\hat x[k{+}1]{=}0{,}6\hat x{+}Bu{+}K(y{-}\hat x)$\\[0.25em]
  Endwert: $f_\infty{=}0{,}36f_\infty-\frac{0{,}36f_\infty^2}{1+f_\infty}+0{,}5$
  $\Rightarrow f_\infty^2+0{,}14f_\infty-0{,}5{=}0$, $f_\infty{=}0{,}641$,
  $K_\infty{=}\frac{0{,}6\cdot0{,}641}{1+0{,}641}{=}0{,}234$.}
\miniexample{Matrix, 3.\ Ordnung, ohne Innovation (erster Schritt)}{%
  $\mathbf A{=}\!\begin{psmallmatrix}0&0&0\\1&0{,}5&0\\0&0&0{,}6\end{psmallmatrix}$,\;
  $\mathbf G{=}\!\begin{psmallmatrix}0&0\\1&0\\0&0{,}8\end{psmallmatrix}$,\;
  $\mathbf C{=}\!\begin{psmallmatrix}0&1&3\end{psmallmatrix}$,\;
  $\mathbf Z_0{=}\!\begin{psmallmatrix}2&0\\0&1\end{psmallmatrix}$,\; $R_0{=}2$.\\[0.2em]
  $\mathbf F[0]{=}\mathbf 0\Rightarrow R[0]{=}R_0{=}2\Rightarrow \mathbf K[0]{=}\mathbf 0$
  $\Rightarrow \mathbf F[1]{=}\mathbf G\,\mathbf Z_0\,\mathbf G^{T}
  {=}\!\begin{psmallmatrix}0&0&0\\0&2&0\\0&0&0{,}64\end{psmallmatrix}$.\quad
  $\dim\mathbf K[1]=3{\times}1$.}
\bereichende
